# dockyards-frontend
SJS Weekend Test

*Window environment may have to install some packages globally*
### Install
```npm install -g```

### Build
```npm run build```

### Run Json-Server
```npm run start:api```

### Run Front-end
```npm run start:dev```
Then go to http://localhost:8080/boats
